services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: gujda
      MYSQL_USER: tomi
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: root_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: